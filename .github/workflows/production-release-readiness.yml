name: Production Release Readiness
run-name: Production Release Readiness · ${{ inputs.target_sha }}

on:
  workflow_dispatch:
    inputs:
      target_sha:
        description: Lowercase 40-character commit SHA to verify
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: production-release-readiness-${{ github.ref_name }}-${{ inputs.target_sha }}
  cancel-in-progress: false

jobs:
  readiness:
    runs-on: ubuntu-latest
    steps:
      - name: Validate workflow ref
        env:
          WORKFLOW_REF: ${{ github.ref }}
        shell: bash
        run: |
          set -euo pipefail

          if [ "$WORKFLOW_REF" != "refs/heads/main" ]; then
            echo "Production release readiness must run from main." >&2
            exit 1
          fi

      - name: Checkout main
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          ref: main
          fetch-depth: 0
          persist-credentials: false

      - name: Validate target commit
        env:
          TARGET_SHA: ${{ inputs.target_sha }}
        shell: bash
        run: |
          set -euo pipefail

          if [[ ! "$TARGET_SHA" =~ ^[0-9a-f]{40}$ ]]; then
            echo "target_sha must be exactly 40 lowercase hexadecimal characters" >&2
            exit 1
          fi

          if ! git cat-file -e "${TARGET_SHA}^{commit}"; then
            echo "target_sha does not identify an existing commit" >&2
            exit 1
          fi

          if ! git merge-base --is-ancestor "$TARGET_SHA" HEAD; then
            echo "target_sha is not contained in main" >&2
            exit 1
          fi

      - name: Verify release images
        env:
          TARGET_SHA: ${{ inputs.target_sha }}
        shell: bash
        run: |
          set -euo pipefail

          verify_image_platforms() {
            local image="$1"

            docker buildx imagetools inspect --raw "$image" \
              | python -c 'import json, sys
index = json.load(sys.stdin)
platforms = {
    (manifest.get("platform", {}).get("os"), manifest.get("platform", {}).get("architecture"))
    for manifest in index.get("manifests", [])
}
missing = {("linux", "amd64"), ("linux", "arm64")} - platforms
if missing:
    raise SystemExit(f"missing required image platforms: {sorted(missing)}")'
          }

          verify_image_platforms "ghcr.io/${GITHUB_REPOSITORY}-backend:${TARGET_SHA}"
          verify_image_platforms "ghcr.io/${GITHUB_REPOSITORY}-frontend:${TARGET_SHA}"

      - name: Write readiness summary
        env:
          TARGET_SHA: ${{ inputs.target_sha }}
        shell: bash
        run: |
          set -euo pipefail

          {
            echo "## Production release readiness"
            echo
            echo "- Target SHA: \`$TARGET_SHA\`"
            echo "- Backend image: ready"
            echo "- Frontend image: ready"
            echo
            echo "Readiness verification is not a deployment success signal."
            echo "The existing OPS-010 manual deployment procedure is the next step."
          } >> "$GITHUB_STEP_SUMMARY"
