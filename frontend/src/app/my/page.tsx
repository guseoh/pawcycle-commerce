"use client";
import Link from "next/link";
import { ErrorState, LoadingState } from "@/components/async-state";
import { useAuth } from "@/lib/auth-context";
import { buildLoginHref } from "@/lib/frontend-utils";
export default function MyPage(){const auth=useAuth();if(auth.status==="anonymous")return <ErrorState title="로그인이 필요합니다." message="내 정보를 보려면 로그인해 주세요."><Link className="button button-primary" href={buildLoginHref("/my")}>로그인</Link></ErrorState>;if(auth.status==="loading")return <LoadingState>회원 정보를 확인하고 있습니다.</LoadingState>;if(auth.status==="error")return <ErrorState title="로그인 상태를 확인할 수 없습니다." message={auth.errorMessage??"다시 시도해 주세요."} onRetry={()=>void auth.refresh()}/>;return <section className="section-card"><h1>내 정보</h1><p>현재 제공되는 사용자 기능으로 이동합니다.</p><div className="button-row"><Link className="button button-secondary" href="/subscriptions">반려동물·정기배송</Link><Link className="button button-secondary" href="/orders">주문</Link><Link className="button button-secondary" href="/wishlist">위시리스트</Link><Link className="button button-secondary" href="/cart">장바구니</Link><Link className="button button-secondary" href="/addresses">배송지</Link><Link className="button button-secondary" href="/billing-methods">결제수단</Link><Link className="button button-secondary" href="/notifications">알림</Link></div></section>;}
